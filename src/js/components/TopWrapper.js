import React from "react";
import OverallStructure from "./OverallStructure"

export default class TopWrapper extends React.Component {

   render() {
        return (
          <div >
            <OverallStructure / >
          </div>
        );
      }
    }
